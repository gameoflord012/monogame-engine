<UserControl x:Class="WPFMonoEditor.UserControls.InspectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFMonoEditor.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ContentControl Template="{StaticResource EditorView}">
            <ScrollViewer>
                <StackPanel>
                    <TextBox x:Name="property1" Style="{StaticResource TextBox:Editable}" 
                             Text="Hello Fuckers" MaxHeight="250"/>       
                    <TextBox x:Name="property2"  Style="{StaticResource TextBox:Editable}" 
                             Text="Hello 2 Fuckers" MaxHeight="250"/>
                </StackPanel>
            </ScrollViewer>
        </ContentControl>
    </Grid>
</UserControl>
